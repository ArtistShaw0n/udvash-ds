import { AnalysisReport } from "@/components/screens/AnalysisReport";

/* Figma V2 — Analysis Report, state A (pending). light 1:5094 · dark 1:5662. frame 376×2280. */
export default function AnalysisReportPage() {
  return <AnalysisReport minHeight={2280} />;
}
