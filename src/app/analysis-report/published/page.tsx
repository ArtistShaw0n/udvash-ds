import { AnalysisReport } from "@/components/screens/AnalysisReport";

/* Figma V2 — Analysis Report, state B (published: Position 5 / Highest 14). light 1:4807 · dark 1:5377. frame 376×2254. */
export default function AnalysisReportPublishedPage() {
  return <AnalysisReport published minHeight={2254} />;
}
